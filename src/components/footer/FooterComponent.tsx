import React, { FC } from 'react';

const FooterComponent: FC = () => {
    return (
        <div>
            <hr/>
            FooterComponent
        </div>
    );
};

export default FooterComponent;