import React, { FC } from 'react';
import './App.css';
import User from './components/User';

const App: FC = () => {
    return (
        <div>
            <User />
        </div>
    );
};

export default App;
