import React, { FC } from 'react';
import './App.css';
import Form from './components/Form/Form';

const App: FC = () => {
    return (
        <div>
            <Form />
        </div>
    );
};

export default App;
